class Solution {
    public boolean isPowerOfTwo(int n) {
        if(n==1)
            return true;
        if(n==Integer.MAX_VALUE||n==Integer.MIN_VALUE)
            return false;
        if(1<<Integer.toBinaryString(n-1).length()==n)
            return true;
        else
            return false;
    }
}
