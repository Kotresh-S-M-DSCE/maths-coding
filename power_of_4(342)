class Solution {
    public boolean isPowerOfFour(int n) {
        if(n<=3&&n!=1)
            return false;
        int rem;
        while(n>1)
        {
            rem=n%4;
            if(rem!=0)
               return false;
            n=n/4;
        }
        return true;
    }
}