class Solution {
    public boolean validDigit(int n, int x) {
        int flag=0;
        while(n>9)
        {
            if(n%10==x)
               flag=1;
            n/=10;
        }
        if(n!=x&&flag==1)
           return true;
        return false;
    }
}