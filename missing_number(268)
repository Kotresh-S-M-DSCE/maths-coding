class Solution {
    public int missingNumber(int[] nums) {
        int dig_sum,arr_sum=0,num;
        dig_sum=nums.length*(nums.length+1)/2;
        for(int i=0;i<nums.length;i++)
            arr_sum+=nums[i];
        num=dig_sum-arr_sum;
        return num;
    }
}