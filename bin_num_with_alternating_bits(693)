class Solution {
    public boolean hasAlternatingBits(int n) {
        while(n>0)
        {
            int temp=n%2;
            n/=2;
            if(temp==n%2)
               return false;
        }
        return true;
    }
}