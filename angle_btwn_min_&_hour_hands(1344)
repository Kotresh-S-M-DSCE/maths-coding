class Solution {
    public double angleClock(int h, int m) {
        if(h==12)
           h=0;
        double hour=(double)h*5*6+(double)m/2;
        double min=m*6;
        double ret=Math.abs(hour-min);
        if(ret>180)
           ret=360-ret;
        return ret;
    }
}